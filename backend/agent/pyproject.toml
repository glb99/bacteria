[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "glb99", email = "guillelb99@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "anyio>=4.15.1",
    "dotenv>=0.9.9",
    "google-genai>=1.0.0",
]

[project.scripts]
bacteria = "bacteria.app.entrypoints.cli:main"

[build-system]
requires = ["uv_build>=0.11.32,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]